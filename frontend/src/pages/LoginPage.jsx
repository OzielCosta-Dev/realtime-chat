// Placeholder — replaced with the real login form in the next sub-step.
// Exists now purely so the router below has something real to render,
// letting us verify the redirect logic end-to-end before building the UI.
export default function LoginPage() {
  return <p>Login page (coming next)</p>;
}
