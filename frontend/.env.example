# Copy to .env.local for local development. Vite only exposes variables
# prefixed with VITE_ to browser code — anything else stays server-side-only
# (a deliberate guard rail so you don't accidentally ship a secret to the
# client bundle).
VITE_API_URL=http://localhost:3001
