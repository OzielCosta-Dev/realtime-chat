# Copy this file to .env and fill in real values.
# .env is gitignored — .env.example is committed so other devs know what to set.

# Server
PORT=3001

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chat_app
DB_USER=postgres
DB_PASSWORD=

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=7d
